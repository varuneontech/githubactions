package com.example.eon.crm.enums;

public enum NotificationChannel {
    EMAIL,      // Notifications sent via email
    SMS,        // Notifications sent via SMS
    APP,        // In-app notifications
    PUSH        // Push notifications to mobile devices
}
