package com.example.eon.crm.enums;

public enum SessionType {
    GROUP,     // Group training session
    PERSONAL,  // Personal training session
    CLASS,     // Regular class session
    TRIAL      // Trial session
}
